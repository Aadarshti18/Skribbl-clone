# Port the Express/Socket.IO server listens on
PORT=3001

# Comma-separated list of allowed client origins for CORS + Socket.IO
# Add your deployed frontend URL here in production, e.g.:
# CLIENT_ORIGIN=http://localhost:5173,https://your-skribbl-clone.vercel.app
CLIENT_ORIGIN=http://localhost:5173
